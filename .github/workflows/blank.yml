name: Development Test and Build and Push Docker Image

on:
  pull_request:
    paths-ignore:
      - '**/README.md'
      - '**/.github/**'
      - '**/package.sh'
    types:
      - synchronize
  check_suite:
    types:
      - requested
      - opened
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write 

jobs:
  check_version_changes:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Check for VERSION changes
        id: check_version
        run: |
          git fetch origin ${{ github.base_ref }}
          git diff --name-only origin/${{ github.base_ref }} HEAD | grep VERSION || echo "VERSION not changed"

  build-and-push:
    needs: check_version_changes
    runs-on: ubuntu-latest
    if: ${{ steps.check_version.outputs.check_version != 'VERSION not changed' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download latest.zip
        run: echo "test"

  build:
    needs: check_version_changes
    runs-on: ubuntu-latest
    if: ${{ steps.check_version.outputs.check_version == 'VERSION not changed' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      # Add steps for the build process
